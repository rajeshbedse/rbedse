name: Validate NSE Sources

on:
  push:
    branches: ["nse-normalization"]
  workflow_dispatch:
    inputs:
      symbols:
        description: "NSE symbols, comma separated"
        required: true
        default: "APLLTD,GANDHAR,RELIANCE,ASHOKLEY,ANANTRAJ"

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install playwright
          python -m playwright install --with-deps chromium

      - name: Run NSE validation
        env:
          VALIDATION_SYMBOLS: ${{ inputs.symbols || 'APLLTD,GANDHAR,RELIANCE,ASHOKLEY,ANANTRAJ' }}
        run: |
          IFS=',' read -ra SYMBOLS <<< "$VALIDATION_SYMBOLS"
          xvfb-run -a python -m nse_pipeline.validate_nse_sources "${SYMBOLS[@]}"

      - name: Upload validation output
        uses: actions/upload-artifact@v4
        with:
          name: nse-validation
          path: output/nse_validation/
          if-no-files-found: error
